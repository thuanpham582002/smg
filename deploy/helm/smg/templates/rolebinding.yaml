{{- if or .Values.rbac.create .Values.router.serviceDiscovery.enabled .Values.router.serviceDiscovery.crds.enabled .Values.router.mesh.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.router.serviceDiscovery.clusterWide }}
kind: ClusterRoleBinding
{{- else }}
kind: RoleBinding
{{- end }}
metadata:
  name: {{ include "smg.fullname" . }}
  labels:
    {{- include "smg.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  {{- if .Values.router.serviceDiscovery.clusterWide }}
  kind: ClusterRole
  {{- else }}
  kind: Role
  {{- end }}
  name: {{ include "smg.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "smg.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
